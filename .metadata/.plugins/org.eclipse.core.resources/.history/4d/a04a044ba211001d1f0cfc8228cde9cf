package com.client.LazardoClient.DAO;

import com.client.LazardoClient.Model.ClientLogin;

import org.apache.ibatis.annotations.Insert;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Result;
import org.apache.ibatis.annotations.Results;

@Mapper
public interface SigninClientRepo {
	
	@Insert("INSERT INTO tbclientlogin (client_id, client_username, client_password) "
			      + "VALUES ( #{loginId},#{username}, #{password} )"
			      +"INSERT INTO tbclientdetails(client_id, client_firstname,client_lastname,client_email,money_balance)"
			      +"VALUES( #{loginId},#{firstname},#{lastname},#{email},#{balance},)"
			)
	@Results(value = {
			@Result(property = "", column = "client_id"),
			@Result(property = "", column = "client_username"),
			@Result(property = "", column = "client_password"),
			@Result(property = "", column = "client_id")
	})
	public Integer signInClient(ClientLogin clientLogin);

}
